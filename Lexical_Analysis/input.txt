begin program:

begin VarDecl:
    (num1, int);
    (num2, int);
    (sum, int);
end VarDecl

num1:=(10, 2);
num2:=(131, 10);

scan("@, @, @", a, b, c);

// Perform addition
sum := num1 + num2;

// Display the result
print("The values are @, @, and @: ", a, b, c);
end program