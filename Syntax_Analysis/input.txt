begin program:

begin VarDecl:
    (number, int);
    (sum, int);

end VarDecl

number := (5, 10);
sum := (0, 10);
scan("@", sum);
b:=(20, 10);
for a := (10, 8) to b + (10, 2) inc (1, 10) do
    begin
        print("The value of a = @", a);
        scan("@, @", b);
    end;
end program
    
print("Final sum = @", sum);
end program